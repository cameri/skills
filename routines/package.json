{
  "name": "claude-channel-routines",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "bin": "./server.ts",
  "omp": {
    "extensions": [
      "./extensions/omp-channel.ts",
      "./extensions/settings-mirror.ts"
    ],
    "settings": {
      "timezone": {
        "type": "string",
        "default": "America/Toronto",
        "description": "Timezone for schedule expressions, IANA name (e.g. America/Toronto). Mirrored into ~/.claude/channels/routines/config.json, which the scheduler reads and which wins if the two disagree."
      },
      "paused": {
        "type": "boolean",
        "default": false,
        "description": "Suspend all firing without losing jobs. Mirrored into ~/.claude/channels/routines/config.json."
      }
    }
  },
  "scripts": {
    "start": "bun install --no-summary && bun server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "croner": "^9.0.0"
  }
}
